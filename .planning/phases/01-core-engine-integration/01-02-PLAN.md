---
phase: 1
plan: 2
wave: 2
depends_on: [1]
agent: Senior Developer
must_haves:
  - PossibleInstrumentsForSong verified for EliteDrums game mode
  - Song scanning detects Elite Drums availability
  - DrumsPlayer.CreateEngine() works with EliteDrums game mode
  - YARG.Core unit tests pass
---

<objective>
Verify and fix the engine wiring and song availability pipeline so that EliteDrums game mode is fully selectable and functional through the downchart path. Run tests to confirm no regressions.
</objective>

<context>
- Read `.planning/phases/01-core-engine-integration/01-CONTEXT.md` for architecture decision
- Read Plan 1 output summary for SongChart changes already applied
- Key files in YARG.Core: `InstrumentEnums.cs`, `AvailableParts.cs`, `MidiEliteDrumsPreparser.cs`, `SongEntry.Scanning.cs`
- Key files in Unity: `Assets/Script/Gameplay/Player/DrumsPlayer.cs`, `Assets/Script/Gameplay/GameManager.Loading.cs`
- The downchart approach means `Player.Profile.CurrentInstrument` will be one of FourLaneDrums/ProDrums/FiveLaneDrums (NOT EliteDrums), while `Player.Profile.GameMode` is `GameMode.EliteDrums`
</context>

<tasks>

## Task 1: Verify song scanning and instrument availability

**Action**: Trace the song scanning pipeline for Elite Drums to ensure songs with Elite Drums charts appear in the music library:

1. Read `YARG.Core/YARG.Core/Song/MidiPreparsers/MidiEliteDrumsPreparser.cs` — understand what difficulty masks it returns. The earlier analysis says it returns TWO masks: one for Elite Drums and one for downchart. Verify this.

2. Read `YARG.Core/YARG.Core/Song/Entries/SongEntry.Scanning.cs` — find where `MidiEliteDrumsPreparser` is called. Verify it sets BOTH `AvailableParts.EliteDrums` AND the downchart instrument parts (FourLaneDrums/FiveLaneDrums).

3. Read `YARG.Core/YARG.Core/Song/Entries/AvailableParts/AvailableParts.cs` — verify `EliteDrums` field exists and is populated during scanning.

4. Read `YARG.Core/YARG.Core/InstrumentEnums.cs` — examine `PossibleInstrumentsForSong()` (around line 232-249). Verify the logic:
   - When `GameMode.EliteDrums`, it returns FiveLaneDrums or FourLaneDrums/ProDrums based on song availability
   - `Instrument.EliteDrums` is intentionally commented out (correct for Phase 1 downchart approach)
   - The returned instruments must match what `AvailableParts` has set from scanning

5. **Critical check**: If a song has ONLY an Elite Drums MIDI track (no native 4L/5L), does the preparser set the downchart availability flags? If not, songs with only Elite Drums charts will be invisible in the music library when EliteDrums game mode is selected. This would be a blocking bug — fix it if found.

**Verify**: Read through the scanning code path end-to-end and document the findings.

**Done**: Song scanning correctly populates availability for Elite Drums and its downchart instruments, OR blocking issues identified and fixed.

## Task 2: Verify DrumsPlayer engine creation for EliteDrums

**Action**: Verify the player initialization path when GameMode is EliteDrums:

1. Read `Assets/Script/Gameplay/GameManager.Loading.cs` — find the prefab selection for EliteDrums (around line 413-415). Confirm it selects `_fiveLaneDrumsPrefab` or `_fourLaneDrumsPrefab` based on song content.

2. Read `Assets/Script/Gameplay/Player/DrumsPlayer.cs`:
   - `Initialize()` (line 51-57): Sets `_fiveLaneMode` from `CurrentInstrument`. When GameMode=EliteDrums and CurrentInstrument=FiveLaneDrums, this should be `true`. Verify.
   - `GetNotes()` (line 59-64): Calls `chart.GetDrumsTrack(Player.Profile.CurrentInstrument)`. Since CurrentInstrument is 4L/5L/Pro (not EliteDrums), this should work. Verify.
   - `CreateEngine()` (line 66-114): The switch on `CurrentInstrument` must NOT throw. Since CurrentInstrument is 4L/5L/Pro, the existing cases should match. The `isMidiDrumsInput` flag at line 87 checks `GameMode is GameMode.EliteDrums` — confirm this is correct.

3. If any code path would throw or misbehave when GameMode=EliteDrums, apply the minimal fix.

4. **Edge case**: Check what happens if `Player.Profile.CurrentInstrument` is somehow set to `Instrument.EliteDrums` (e.g., from a stale profile). Both `GetNotes()` and `CreateEngine()` would throw. Consider adding a guard or logging, but do NOT add `Instrument.EliteDrums` as a case in the engine mode switch — that requires a native engine (Phase 3).

**Verify**: Read the full code path and confirm no exceptions would occur for the standard EliteDrums game mode flow.

**Done**: DrumsPlayer correctly creates an engine for EliteDrums game mode via the downchart path.

## Task 3: Run YARG.Core unit tests

**Action**: Run the full test suite and verify:

1. Execute:
   ```bash
   cd YARG.Core && dotnet test YARG.Core.UnitTests/YARG.Core.UnitTests.csproj --verbosity normal
   ```

2. All existing tests must pass. If any fail, investigate whether the failure is pre-existing or caused by Plan 1 changes.

3. Check if any existing tests cover Elite Drums paths:
   - Search test files for "EliteDrum" or "elite" references
   - If no coverage exists, note this as a gap but do NOT write new tests unless a specific bug is found

4. If tests fail due to Plan 1 changes (SongChart.Append or timing methods), fix the root cause.

**Verify**: Test output shows all tests passing.

**Done**: YARG.Core unit tests pass with no regressions. Test coverage gaps for Elite Drums documented.

</tasks>

<verification>
```bash
cd YARG.Core && dotnet test YARG.Core.UnitTests/YARG.Core.UnitTests.csproj --verbosity normal
```
All tests must pass. Zero failures, zero errors.
</verification>

<success_criteria>
- Song scanning pipeline verified: Elite Drums charts result in correct AvailableParts population
- PossibleInstrumentsForSong returns valid instruments for EliteDrums game mode
- DrumsPlayer.CreateEngine() confirmed working for EliteDrums via downchart path
- GameManager.Loading.cs prefab selection verified for EliteDrums
- All YARG.Core unit tests pass
- No regressions introduced
</success_criteria>

<output>
Summary of verification findings, any fixes applied, and test results. Include specific line numbers for any changes made.
</output>

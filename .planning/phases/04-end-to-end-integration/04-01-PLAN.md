---
phase: 4
plan: 1
wave: 1
depends_on: []
agent: Senior Developer
must_haves:
  - Uncomment Instrument.EliteDrums in PossibleInstrumentsForSong
  - Add EliteDrums case to ModifierIcon.SpawnEnginePresetIcons
  - Fix pre-existing ModifierIcon bug (FiveFretGuitar preset used for drums)
  - Verify no other missing switch cases or references
  - Check EliteDrumsPlayer null safety for partial chart difficulties
---

<objective>
Enable Elite Drums song selection and fix all remaining integration gaps. After this plan, Elite Drums is selectable as an instrument in the difficulty menu, and all switch statements/enum references properly handle GameMode.EliteDrums.
</objective>

<context>
- Phase 4 context: `.planning/phases/04-end-to-end-integration/04-CONTEXT.md`
- Prior phase outputs: Phases 1-3 complete (engine, bindings, visual highway all wired)
- CODEBASE.md: `.planning/CODEBASE.md`
- Architecture: downchart-first (engine uses DrumNote, EliteDrumsPlayer supplements with EliteDrumNote for visuals)
</context>

<tasks>

## Task 1: Enable Elite Drums Instrument Selection

**What**: Uncomment `Instrument.EliteDrums` in `PossibleInstrumentsForSong()` so that the difficulty select menu shows Elite Drums as a selectable instrument when the player's game mode is EliteDrums.

**Action**:
1. Open `YARG.Core/YARG.Core/InstrumentEnums.cs`
2. Line 240: Uncomment `Instrument.EliteDrums` in the FiveLaneDrums-available branch
3. Line 245: Uncomment `Instrument.EliteDrums` in the FourLaneDrums/ProDrums branch
4. Verify the array syntax is correct (trailing commas per code style)

**Why this works**: `GetDrumsTrack(Instrument.EliteDrums)` already returns the FourLaneDrums downchart (Phase 1 wiring). `entry.HasInstrument(Instrument.EliteDrums)` returns true for songs with PART ELITE DRUMS charts (preparser wired in Phase 1). EliteDrumsPlayer separately loads the EliteDrumNote track for visual correlation.

**Verify**:
```bash
cd YARG.Core && dotnet build YARG.Core/YARG.Core.csproj
```

**Done**: `Instrument.EliteDrums` appears in both branches of the `GameMode.EliteDrums` case in `PossibleInstrumentsForSong()`.

## Task 2: Add EliteDrums to ModifierIcon

**What**: Add `case GameMode.EliteDrums:` alongside `FourLaneDrums`/`FiveLaneDrums` in `ModifierIcon.SpawnEnginePresetIcons()` so that drum modifier icons (Dynamic Hit Window, No Star Power Overlap) display on the score screen for Elite Drums.

**Action**:
1. Open `Assets/Script/Menu/ScoreScreen/ScoreCards/ModifierIcon.cs`
2. At line 75, add `case GameMode.EliteDrums:` before `case GameMode.FiveLaneDrums:`
3. This falls through to the existing drums modifier icon logic
4. **CRITIQUE MITIGATION**: Fix pre-existing bug on line 85 -- change `enginePreset.FiveFretGuitar.NoStarPowerOverlap` to `enginePreset.Drums.NoStarPowerOverlap`. This bug affects ALL drum modes (FourLaneDrums, FiveLaneDrums), not just EliteDrums. It incorrectly checks the guitar preset for the "No Star Power Overlap" modifier on the drums score card.

**Verify**: Visual inspection -- the switch case now includes EliteDrums alongside other drum modes, and the NoStarPowerOverlap check uses the correct `Drums` preset.

**Done**: `ModifierIcon.SpawnEnginePresetIcons()` handles `GameMode.EliteDrums` with the same modifier icons as other drum modes, and the pre-existing preset field bug is fixed.

## Task 3: Integration Sweep

**What**: Sweep the entire codebase for any remaining missing EliteDrums references, commented-out code that should be active, or switch statements that might throw on EliteDrums.

**Action**:
1. Search for all `//.*EliteDrums` patterns -- find any commented-out Elite Drums code that should now be active
2. Search for all `switch` statements on `GameMode` or `Instrument` that handle drums but not Elite Drums
3. Search for `ThemeComponent` or any VisualStyle switches that might throw on unhandled `VisualStyle.EliteDrums`
4. Check `GameModeExtensions.cs` for any missing EliteDrums handling
5. Verify `DifficultySelectMenu.HasPlayableInstrument()` works with `Instrument.EliteDrums` (it uses generic `entry.HasInstrument()` -- should work)
6. Fix any gaps found

**Important edge cases to check**:
- `BindingCollection.Defaults` -- any game mode switches
- `TrackViewManager` -- track allocation for EliteDrums
- `GameManager.Debug.cs` -- any debug-only switches
- `Replay` code paths -- does replay recording handle EliteDrums game mode?
- **CRITIQUE MITIGATION**: Check `EliteDrumsPlayer` null safety for partial Elite Drums charts. Specifically, verify that `GetDifficulty()` on the EliteDrumNote track handles the case where a difficulty doesn't exist in Elite (e.g., song only has Expert Elite Drums but player selects Easy). The `BuildCorrelationMap()` method should gracefully handle null `_eliteDifficulty` (check if existing null guard at line ~105 is sufficient).

**Verify**:
```bash
cd YARG.Core && dotnet build YARG.Core/YARG.Core.csproj
# Visual inspection of all changed files
```

**Done**: No remaining commented-out EliteDrums code that should be active, and no switch statements that would throw or silently skip EliteDrums.

</tasks>

<verification>
1. YARG.Core builds without errors
2. `PossibleInstrumentsForSong()` returns `Instrument.EliteDrums` in its array for `GameMode.EliteDrums`
3. `ModifierIcon.SpawnEnginePresetIcons()` handles `GameMode.EliteDrums`
4. No remaining integration gaps found in sweep
</verification>

<success_criteria>
- Elite Drums instrument is selectable in the difficulty select menu
- Score screen modifier icons display correctly for Elite Drums
- No switch statements throw or silently skip EliteDrums
- YARG.Core builds and all existing tests pass
</success_criteria>

<output>
Summary of all changes made, files modified, and any issues discovered during the integration sweep.
</output>

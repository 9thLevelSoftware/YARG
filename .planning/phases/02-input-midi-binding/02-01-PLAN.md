---
phase: 2
plan: 1
wave: 1
depends_on: []
must_haves:
  - Analyze downchart mapping as source of truth for Elite→DrumsAction conversion
  - Uncomment all 13 Elite-specific bindings in CreateEliteDrumsBindings()
  - Remove 4L/5L fallback bindings from CreateEliteDrumsBindings()
  - Add Elite→DrumsAction mappings in ConvertMidiDrumsInput() verified against downchart
  - Remove Elite→4L/5L case from FriendlyBindingDialog.GetHighlightByName()
agents:
  - legion:Senior Developer
status: pending
critique_mitigations:
  - Downchart mapping analysis before writing engine conversions (pre-mortem finding #2)
  - Keep FourLane*/FiveLane* enum cases in ConvertMidiDrumsInput for replay compat (pre-mortem finding #3)
  - Cross-reference engine conversion with MoonSongLoader.EliteDrumsDownchart.cs (assumption #3)
---

<objective>
Enable the real Elite Drums binding infrastructure with downchart-verified engine mappings. First analyze the downchart source of truth, then atomically uncomment Elite bindings, remove 4L/5L fallbacks, and add verified engine mappings.

CRITICAL: The engine conversion mappings (ConvertMidiDrumsInput) MUST match the downchart generator (MoonSongLoader.EliteDrumsDownchart.cs). A mismatch means bound inputs won't register during gameplay. Analyze the downchart first, then use it as the authoritative source.
</objective>

<context>
- Phase context: `.planning/phases/02-input-midi-binding/02-CONTEXT.md`
- Spec document: `.planning/specs/02-input-midi-binding-spec.md`
- Phase 1 completed: Engine wiring, SongChart integration, downchart architecture
- Architecture: Downchart-first — EliteDrumsAction inputs are converted to DrumsAction for the existing DrumsEngine
- Key constraint: Removing 4L/5L fallback bindings WITHOUT adding Elite→DrumsAction mappings will break gameplay
- Critique finding: No single source of truth links ConvertMidiDrumsInput() to MoonSongLoader.EliteDrumsDownchart.cs — analysis step required
- Critique finding: Keep FourLane*/FiveLane* enum cases in engine for replay backward compatibility
</context>

<tasks>

## Task 1: Analyze Downchart Mapping as Source of Truth

**Files to read**:
- `YARG.Core/YARG.Core/Chart/Loaders/MoonSong/MoonSongLoader.EliteDrums.cs` — the downchart generator
- `YARG.Core/YARG.Core/Engine/Drums/Engines/YargDrumsEngine.cs` — current ConvertMidiDrumsInput()
- `YARG.Core/YARG.Core/Input/InputActions.cs` — EliteDrumsAction and DrumsAction enums
- `YARG.Core/YARG.Core/Chart/Notes/DrumNote.cs` — DrumPad enum values used in downchart

### Action
1. Read `MoonSongLoader.EliteDrumsDownchart.cs` completely. Find the method that converts Elite drum pads to 4-lane/5-lane gems (likely `DownchartEliteDrumsChord()` or similar).
2. Extract the complete mapping table: for each EliteDrumPad, which DrumPad (4-lane) does it map to? Document:
   ```
   EliteDrumPad.Snare → DrumPad.Red (Drum1)
   EliteDrumPad.ClosedHiHat → DrumPad.Yellow (Drum2) [cymbal in ProDrums]
   ...etc for all pads
   ```
3. Read the existing `ConvertMidiDrumsInput()` in YargDrumsEngine.cs. Document which EliteDrumsAction values it currently handles and which it doesn't.
4. Read `InputActions.cs` to verify the EliteDrumsAction enum values haven't changed since Phase 1.
5. Cross-reference: Create a verified mapping table that connects:
   - EliteDrumPad (chart note) → DrumPad (downcharted note) → DrumsAction (engine action)
   - EliteDrumsAction (input action) → DrumsAction (engine conversion)
6. Identify any edge cases:
   - ProDrums vs non-ProDrums (cymbal vs pad distinction)
   - Actions that map to null (Stomp, Splash — no 4-lane equivalent)
   - The 5-lane downchart path (if it exists)

### Verify
- Complete mapping table covers all 13 EliteDrumsAction values
- Each mapping is traced from downchart source code, not assumed
- ProDrums/non-ProDrums distinction is documented
- Edge cases (Stomp/Splash → null) are explicitly listed

### Done
A verified Elite→DrumsAction mapping table exists, derived directly from the downchart source code, ready to be implemented in ConvertMidiDrumsInput().

---

## Task 2: Enable Elite Bindings and Engine Mappings (Atomic)

**Files to modify**:
- `Assets/Script/Input/Bindings/BindingCollection.Templates.cs` — binding template
- `YARG.Core/YARG.Core/Engine/Drums/Engines/YargDrumsEngine.cs` — engine conversion

### Action
**IMPORTANT**: Both files must be modified together. Removing 4L/5L bindings without adding engine mappings breaks gameplay. Adding engine mappings without the bindings is harmless but incomplete.

**Part A — BindingCollection.Templates.cs:**
1. Read `CreateEliteDrumsBindings()` method
2. Uncomment all 13 Elite-specific `DrumPadButtonBinding` entries:
   - Keep `"Drums.Kick"` → `EliteDrumsAction.Kick`
   - Uncomment: Stomp, Splash, Snare, ClosedHiHat, SizzleHiHat, OpenHiHat, LeftCrash, Tom1, Tom2, Tom3, Ride, RightCrash
3. Remove ALL `FourLane*` and `FiveLane*` fallback bindings from this method
4. Verify binding name strings match localization keys in `en-US.json`

**Part B — YargDrumsEngine.cs:**
1. Read `ConvertMidiDrumsInput()` method
2. Using the verified mapping table from Task 1, add Elite→DrumsAction mappings:
   - In the `FourLaneDrums/ProDrums` branch: add all Elite-specific action cases
   - In the `FiveLaneDrums` branch: add equivalent 5-lane mappings
   - For ProDrums target: use cymbal variants (Drum2Cymbal, etc.) where the downchart maps to cymbals
   - For non-ProDrums: use pad variants (Drum2, etc.)
   - EliteStomp → null (no 4-lane equivalent)
   - EliteSplash → null (no 4-lane equivalent)
3. **KEEP existing FourLane*/FiveLane* cases** in the switch statement for backward compatibility of saved replays, even though the binding template no longer creates them
4. Add a comment block documenting the mapping source:
   ```
   // Elite→DrumsAction mappings verified against MoonSongLoader.EliteDrumsDownchart.cs
   // See .planning/phases/02-input-midi-binding/02-CONTEXT.md for mapping table
   ```

### Verify
- `CreateEliteDrumsBindings()` has exactly 13 bindings (Kick + 12 Elite-specific)
- No 4L/5L references remain in the binding template method
- `ConvertMidiDrumsInput()` handles ALL EliteDrumsAction enum values (13 Elite + old FourLane/FiveLane for compat)
- Each Elite→DrumsAction mapping matches the downchart table from Task 1
- Run YARG.Core unit tests: `cd YARG.Core && dotnet test YARG.Core.UnitTests/YARG.Core.UnitTests.csproj`

### Done
Elite bindings are active and the engine conversion is verified against the downchart source of truth. Old FourLane/FiveLane cases retained for replay compat.

---

## Task 3: Clean Up Base Dialog Class and Sweep for Orphans

**File to modify**: `Assets/Script/Menu/Common/Dialogs/Onboarding/FriendlyBindingDialog.cs`

### Action
1. Read `FriendlyBindingDialog.cs` — find the `GetHighlightByName()` method
2. Remove the entire `GameMode.EliteDrums` case block (maps Elite mode to 4L/5L highlight indices)
3. Check for any other Elite→4L/5L references in the file and remove them
4. **Orphan sweep** — search the entire codebase for references to the removed 4L/5L Elite Drums pattern:
   - Search for `EliteDrumsAction.FourLane` in files OTHER than `InputActions.cs` and `YargDrumsEngine.cs`
   - Search for `EliteDrumsAction.FiveLane` in files OTHER than `InputActions.cs` and `YargDrumsEngine.cs`
   - Search for `SetFiveLaneDrums` anywhere
   - Search for `_fourLaneDrumKit` and `_fiveLaneDrumKit` in context of Elite Drums
   - Flag any orphaned references that need cleanup (these may be in files outside the plan scope)
5. Run YARG.Core unit tests to confirm no regressions

### Verify
- No `EliteDrums` references remain in FriendlyBindingDialog.cs
- Other drum modes (FourLaneDrums, FiveLaneDrums) still have their cases intact
- Orphan sweep results documented (clean or list of references to address)
- Unit tests pass

### Done
FriendlyBindingDialog.cs is clean of Elite→4L/5L logic. Orphan sweep completed and documented.

</tasks>

<verification>
1. Verified mapping table: Elite→DrumsAction matches downchart generator for all 13 actions
2. YARG.Core unit tests pass: `cd YARG.Core && dotnet test YARG.Core.UnitTests/YARG.Core.UnitTests.csproj`
3. `CreateEliteDrumsBindings()` returns exactly 13 bindings
4. `ConvertMidiDrumsInput()` handles all EliteDrumsAction values (Elite + legacy FourLane/FiveLane)
5. `FriendlyBindingDialog.cs` has no Elite→4L/5L case
6. No orphaned 4L/5L Elite references found in codebase (or documented if found)
</verification>

<success_criteria>
- Downchart mapping analyzed and documented as source of truth
- All 13 Elite Drums bindings active in binding template
- 4L/5L fallback bindings removed from template
- Engine conversion verified against downchart (not guessed)
- Old FourLane/FiveLane enum cases kept in engine for replay compat
- Base dialog class clean of Elite-specific 4L/5L logic
- All existing tests pass
- No orphaned references to old Elite→4L/5L pattern
</success_criteria>

<output>
Plan summary documenting:
1. Verified Elite→DrumsAction mapping table (from downchart analysis)
2. Binding template changes (before/after)
3. Engine conversion changes (all new cases added)
4. Orphan sweep results
5. Test results
</output>

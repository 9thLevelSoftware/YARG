---
phase: 2
plan: 2
wave: 2
depends_on: [02-01]
must_haves:
  - Rewrite EliteDrumsBindingDialog for 13-action Elite binding flow
  - Define serialized fields for highlight images with runtime validation
  - Override GetHighlightByName, IsKeyValid, Initialize for Elite-specific logic
  - Verify end-to-end binding-to-engine pipeline with downchart consistency check
agents:
  - legion:Senior Developer
status: pending
critique_mitigations:
  - Runtime validation for highlight array size (pre-mortem finding #4)
  - Removed mode-switching entirely instead of refactoring (pre-mortem finding #5)
  - End-to-end pipeline verification includes downchart cross-reference (assumption #3)
---

<objective>
Rewrite the Elite Drums binding dialog to use the full 13-action Elite Drums binding flow. Define serialized field structure with runtime validation, document prefab wiring, and verify the complete binding-to-engine pipeline including downchart consistency.
</objective>

<context>
- Phase context: `.planning/phases/02-input-midi-binding/02-CONTEXT.md`
- Spec document: `.planning/specs/02-input-midi-binding-spec.md`
- Plan 02-01 completed — what changed:
  - Verified Elite→DrumsAction mapping table exists (derived from downchart analysis)
  - `BindingCollection.Templates.cs` now has 13 Elite-specific DrumPadButtonBinding entries (4L/5L fallbacks removed)
  - `YargDrumsEngine.cs` ConvertMidiDrumsInput() maps all Elite actions to DrumsAction (verified against downchart)
  - `FriendlyBindingDialog.cs` no longer has Elite→4L/5L case in GetHighlightByName()
  - Orphan sweep completed
- Reference pattern: `FourLaneDrumsBindingDialog.cs` and `FiveLaneDrumsBindingDialog.cs` — simple subclasses of FriendlyBindingDialog
- Base class API: `FriendlyBindingDialog.cs` provides `BindingLoop()`, `CheckForModeSwitch()`, `IsKeyValid()`, `GetHighlightByName()` virtual methods
- Critique mitigation: Runtime validation for highlight array mismatch, no mode-switching
</context>

<tasks>

## Task 1: Rewrite EliteDrumsBindingDialog.cs

**File**: `Assets/Script/Menu/Common/Dialogs/Onboarding/EliteDrumsBindingDialog.cs`

### Action
1. Read the current file completely
2. Read `FourLaneDrumsBindingDialog.cs` and `FiveLaneDrumsBindingDialog.cs` to understand the simple subclass pattern
3. Read `FriendlyBindingDialog.cs` to understand the base class API:
   - How `BindingLoop(GameMode)` iterates bindings and calls virtual methods
   - How `GetHighlightByName()` is used to activate/deactivate highlight images
   - How `IsKeyValid()` filters which binding keys to show
   - How `CheckForModeSwitch()` is called during the loop
4. Rewrite `EliteDrumsBindingDialog.cs`:

**Remove entirely:**
- `_fourLaneDrumKitImage`, `_fiveLaneDrumKitImage` serialized fields
- `_fourLaneDrumKitHighlights`, `_fiveLaneDrumKitHighlights` arrays
- `_currentMode` enum/field
- `SetFiveLaneDrums()` method
- All 4L/5L mode-switching logic in `CheckForModeSwitch()`
- Any `using` directives only needed for removed code

**Add:**
```csharp
// Prefab wiring (FriendlyEliteDrumsBindingDialog.prefab):
// _eliteDrumHighlights array must have 13 elements:
//   [0] Kick, [1] Stomp, [2] Splash, [3] LeftCrash,
//   [4] ClosedHiHat, [5] SizzleHiHat, [6] OpenHiHat,
//   [7] Snare, [8] Tom1, [9] Tom2, [10] Tom3,
//   [11] Ride, [12] RightCrash

[Header("Elite Drums Kit")]
[SerializeField] private Image _drumKitImage;

[Header("Elite Drums Highlights (13 pads)")]
[SerializeField] private Image[] _eliteDrumHighlights;
```

**Override methods:**

a. `Initialize()`:
   - Add runtime validation:
     ```csharp
     if (_eliteDrumHighlights == null || _eliteDrumHighlights.Length != 13)
     {
         Debug.LogError($"EliteDrumsBindingDialog: Expected 13 highlights, " +
             $"got {_eliteDrumHighlights?.Length ?? 0}. Check prefab wiring.");
     }
     ```
   - Set dialog message text for Elite Drums context
   - Call base initialization
   - Start binding loop with `GameMode.EliteDrums`

b. `GetHighlightByName(GameMode mode, string bindingName)`:
   - Map binding names to highlight array indices using a switch expression:
     ```
     "Drums.Kick" → _eliteDrumHighlights[0]
     "EliteDrums.Stomp" → _eliteDrumHighlights[1]
     "EliteDrums.Splash" → _eliteDrumHighlights[2]
     "EliteDrums.LeftCrash" → _eliteDrumHighlights[3]
     "EliteDrums.ClosedHiHat" → _eliteDrumHighlights[4]
     "EliteDrums.SizzleHiHat" → _eliteDrumHighlights[5]
     "EliteDrums.OpenHiHat" → _eliteDrumHighlights[6]
     "EliteDrums.Snare" → _eliteDrumHighlights[7]
     "EliteDrums.Tom1" → _eliteDrumHighlights[8]
     "EliteDrums.Tom2" → _eliteDrumHighlights[9]
     "EliteDrums.Tom3" → _eliteDrumHighlights[10]
     "EliteDrums.Ride" → _eliteDrumHighlights[11]
     "EliteDrums.RightCrash" → _eliteDrumHighlights[12]
     _ → null (with Debug.LogWarning for unrecognized binding names)
     ```
   - IMPORTANT: Verify binding names match EXACTLY what `CreateEliteDrumsBindings()` uses in BindingCollection.Templates.cs

c. `IsKeyValid(GameMode mode, string key)`:
   - Return true for `"Drums.Kick"` or keys starting with `"EliteDrums."`
   - Return false for everything else (including any "FourLane" or "FiveLane" prefixed keys)

d. `CheckForModeSwitch(string key, GameMode mode)`:
   - Override to do nothing — Elite Drums is a single-mode dialog, no switching

5. Update completion message to Elite Drums context

### Verify
- File compiles without errors
- All 13 binding names have highlight mappings
- Binding names match `CreateEliteDrumsBindings()` exactly (cross-reference the file)
- No references to 4L/5L modes, mode switching, or old field names remain
- Class inherits from `FriendlyBindingDialog`
- Runtime validation in `Initialize()` catches array size mismatches

### Done
EliteDrumsBindingDialog implements a complete 13-action Elite Drums binding flow with runtime validation.

---

## Task 2: Verify Binding Names Consistency

**Files to cross-reference**:
- `Assets/Script/Input/Bindings/BindingCollection.Templates.cs` — binding names in `CreateEliteDrumsBindings()`
- `Assets/Script/Menu/Common/Dialogs/Onboarding/EliteDrumsBindingDialog.cs` — binding names in `GetHighlightByName()`
- `Assets/StreamingAssets/lang/en-US.json` — localization keys under "EliteDrums"

### Action
1. Extract the exact binding name strings from `CreateEliteDrumsBindings()` (e.g., `"Drums.Kick"`, `"EliteDrums.Stomp"`, etc.)
2. Extract the exact binding name strings from `GetHighlightByName()` in the rewritten dialog
3. Extract the localization keys from `en-US.json` under the "EliteDrums" section
4. Cross-reference all three:
   - Every binding template name must have a matching case in `GetHighlightByName()`
   - Every binding template name must have a matching localization key
   - Document any mismatches
5. If mismatches found: fix them in the appropriate file
6. Verify the binding order in the template matches the desired binding dialog order:
   - Kick → Stomp → Splash → LeftCrash → ClosedHiHat → SizzleHiHat → OpenHiHat → Snare → Tom1 → Tom2 → Tom3 → Ride → RightCrash
   - The binding loop iterates in template order, so the order in `CreateEliteDrumsBindings()` determines the binding sequence

### Verify
- 100% match between template names, dialog highlight names, and localization keys
- Binding order in template matches desired dialog flow
- No orphaned or missing entries

### Done
All binding names are consistent across template, dialog, and localization. Binding order matches the physical kit layout.

---

## Task 3: End-to-End Pipeline Verification

### Action
1. Run YARG.Core unit tests:
   ```
   cd YARG.Core && dotnet test YARG.Core.UnitTests/YARG.Core.UnitTests.csproj
   ```

2. **Pipeline trace** — verify the complete flow by code review:
   a. `CreateEliteDrumsBindings()` → 13 DrumPadButtonBinding entries with correct EliteDrumsAction values
   b. `DrumPadButtonBinding.FireInputEvent()` → GameInput created with action + velocity
   c. `YargDrumsEngine.MutateStateWithInput()` → receives GameInput, checks `IsMidiDrumsInput`
   d. `ConvertMidiDrumsInput()` → maps EliteDrumsAction to DrumsAction using downchart-verified table
   e. Verify: for each of the 11 playable actions (all except Stomp/Splash), the DrumsAction output matches what the downcharted DrumNote track would contain
   f. Verify: Stomp and Splash return null (expected — no 4-lane equivalent, Phase 3 will handle)

3. **Downchart consistency check**:
   - For each Elite pad that maps to a DrumsAction in `ConvertMidiDrumsInput()`:
     - Verify the corresponding EliteDrumNote in the chart would be downcharted to the same DrumPad/DrumsAction
     - Example: if `EliteSnare → DrumsAction.Drum1`, verify that `EliteDrumPad.Snare` downcharts to `DrumPad.Red`
   - This check uses the mapping table from Plan 1 Task 1

4. **Orphan sweep** — search for any remaining old-pattern references:
   - `SetFiveLaneDrums` anywhere in codebase
   - `_fourLaneDrumKit` or `_fiveLaneDrumKit` in Elite Drums context
   - `FourLaneRedDrum` in binding template or dialog code (should only remain in InputActions.cs enum and YargDrumsEngine.cs compat cases)

5. **Localization completeness**:
   - Every binding name in `CreateEliteDrumsBindings()` has a key in `en-US.json`
   - Check other language files if they exist (they may have placeholders)

### Verify
- Unit tests pass (0 failures)
- Pipeline trace confirms no action is silently dropped (except Stomp/Splash → null by design)
- Downchart consistency check passes for all 11 playable actions
- Orphan sweep clean (or documented if residual references exist in out-of-scope files)
- Localization complete for all binding names

### Done
End-to-end pipeline verified: binding→GameInput→engine→DrumsAction. Downchart consistency confirmed. No orphaned references.

</tasks>

<verification>
1. YARG.Core unit tests pass
2. EliteDrumsBindingDialog compiles with 13-action binding flow + runtime validation
3. Binding names 100% consistent across template, dialog, and localization
4. Pipeline trace confirms correct flow from input to engine
5. Downchart consistency verified for all 11 playable Elite actions
6. No orphaned 4L/5L Elite references in codebase
7. Binding dialog order matches physical kit layout
</verification>

<success_criteria>
- Binding dialog implements 13-action Elite Drums flow with runtime validation
- Each binding name matches across template, dialog, and localization (zero mismatches)
- Serialized fields defined with Header attributes and documentation comment
- Complete pipeline verified by code review: binding → GameInput → engine conversion
- Downchart consistency confirmed: engine conversion matches downchart generator
- No regressions in unit tests
- No orphaned 4L/5L Elite Drums references
</success_criteria>

<output>
Plan summary documenting:
1. Dialog rewrite details (removed/added code)
2. Binding name consistency matrix
3. Pipeline trace results
4. Downchart consistency check results
5. Prefab wiring instructions for manual Unity Editor work
6. Test results
</output>
